;;; packages-which-key.el --- Which-key Packages -*- lexical-binding: t; -*-
;;; Commentary:

;;; Code:

(use-package which-key :ensure t :after packages-keybindings)

(use-package which-key-posframe :ensure t :after which-key)

(provide 'packages-which-key)

;;; packages-which-key.el ends here
